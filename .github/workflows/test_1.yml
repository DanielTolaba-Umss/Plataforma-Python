name: for react test

on:
  push:
    branches: ["Semana-1"]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - run: npm ci
      - run: cd front-end
      - run: npm run lint
